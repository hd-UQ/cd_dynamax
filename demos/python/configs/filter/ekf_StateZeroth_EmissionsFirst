[EKF]
dt_final: 1e-4
state_order: zeroth
emission_order: first
smooth_order: first
cov_rescaling: 1.0
diffeqsolve_settings_file: solver/dt1e-2_maxSteps1e5

[filter_info]
name=EKF (State 0th, Emission 1st order)
desc=Extended Kalman Filter with zeroth order approximation to state function and first order approximatin to emission function.
# Plotting styles
filtered_style = {'color': '#0b3c6f', 'linestyle': '-', 'linewidth': 1.5, 'alpha': 0.7}
forecasted_style = {'color': '#0b3c6f', 'linestyle': '--', 'linewidth': 1.5, 'alpha': 0.7}
